{
  "scripts": {
    "coverage": "istanbul cover jasmine",
    "coveralls": "yarn coverage && istanbul-coveralls",
    "test": "yarn jasmine",
    "deploy": "yarn serverless deploy"
  },
  "devDependencies": {
    "eslint": "^5.6.0",
    "istanbul": "^0.4.5",
    "istanbul-coveralls": "^1.0.3",
    "jasmine": "^3.2.0",
    "jasmine-spec-reporter": "^4.2.1",
    "lambda-tester": "^3.5.0",
    "nock": "^9.6.1",
    "serverless": "^1.32.0"
  },
  "dependencies": {
    "@rgrove/parse-xml": "^1.1.1",
    "axios": "^0.18.0"
  }
}
